# SMTP Configuration
# Copy this file to .env and fill in your actual values
# For cPanel Node.js apps, set these in the app's environment variables section

# SMTP Password for no-reply@saibaitour.mn
SMTP_PASSWORD=your_actual_smtp_password_here

# Optional: Override default port (default is 3000)
# PORT=3000

# Optional: Override base path (default is /mail-service)
# BASE_PATH=/mail-service
