# Mail Service Configuration for cPanel
# cPanel will automatically set PORT and NODEJS_PORT

# SMTP Configuration
SMTP_HOST=bizmail22.itools.mn
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your_email@domain.com
SMTP_PASS=your_password

# CORS Configuration - Update with your actual domain
ALLOWED_ORIGINS=https://yourdomain.com,http://localhost:3000

# Environment
NODE_ENV=production
